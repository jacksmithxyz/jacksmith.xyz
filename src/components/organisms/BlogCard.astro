---
const { url, title, date } = Astro.props;
---

<div class="flex flex-col sm:flex-row gap-8">
    <div>
        <h2 class="font-medium text-xl mb-1"><a href={url}>{title}</a></h2>
        <p class="font-mono text-zinc-600 text-xs mb-1">{date}</p>
    </div>
</div>
